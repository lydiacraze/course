version: 2

sources:

  - name: raw
    database: DB_ECOMM
    schema: RAW

    tables:
      - name: raw_customers
      - name: raw_orders
      - name: raw_payments
      - name: raw_products
      - name: raw_order_items